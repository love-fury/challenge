# Discovery Artifacts

이 디렉터리는 현재 static deob 기반 분석 코드만 유지한다.

- `05-static-deob`: bundle mirror, module extraction, heuristic reconstruction, corpus promotion

기존 `01~04` discovery runtime probe 코드는 더 이상 `src`에 유지하지 않는다.
