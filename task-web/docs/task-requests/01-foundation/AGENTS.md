# AGENTS.md

## Deprecated / Archive

- 이 문서는 historical discovery archive다.
- `01-foundation`은 과거 runtime topology와 command surface를 공용 기반 사실로 정리하던 track이었다.
- 현재 구현 지침으로 사용하지 말고, active guidance는 루트 `AGENTS.md`와 `docs/reverse-engineering.md`를 따른다.

## Historical Role

- editor target, iframe/canvas topology, `HwpApp` root, command surface 후보를 폭넓게 정리했다.
- downstream track이 다시 probe할 수 있는 object path, acceptance map, evidence gap을 남기는 역할이었다.
- public SDK surface를 직접 설계하거나 최종 capability pass/fail을 결정하는 track은 아니었다.

## Archive Note

- 이 디렉터리의 `findings.md`, `handoff.md`, `acceptance-map.md`, `exploration-*` 문서는 당시 탐색 맥락 보존용이다.
- 현재 구현에서는 여기의 pending task, owner assignment, audit workflow를 따르지 않는다.
