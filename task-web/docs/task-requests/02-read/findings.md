# 02 Read Findings

Append-only log. 각 항목은 날짜와 `hypothesis` / `observed` / `confirmed` / `failed` 태그를 포함한다.

## 2026-03-29

- `confirmed`: track initialized. Structured read-path와 style mapping 탐색을 이 디렉터리에서 관리한다.
- `observed`: `readText()`는 hook이 없으면 `probeDocumentTextChain()` 결과의 `extractedText`를 사용하고, `readStructure()`는 같은 fallback으로 paragraph node만 복구한다.
- `observed`: `readParagraphFormatting()`는 hook 실패 시 `pageReadParagraphFormattingFromTextChain()` fallback을 사용하며, 현재 `fontName`, `fontSize`, `color`, `lineSpacing`까지만 복구한다.
- `observed`: 현재 paragraph fallback은 text-chain에서 첫 `styleRef`만 채택하므로, multi-run paragraph 내부의 bold/italic/heading/alignment 정보는 아직 구조적으로 복구되지 않는다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-read-runs.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860`가 정상 동작했고, 결과는 `tmp/discovery/02-read/read-run-probe.json`에 저장했다.
- `confirmed`: live 문서에서 `paragraphCount=53`, `textNodeCount=56`, `controlNodeCount=15`, `nodesWithMultipleRunCandidates=51`이 관측됐다. 즉, `Csi`는 대부분의 본문 node에서 다중 pair 후보를 가지므로 현재의 first-style paragraph fallback은 본질적으로 lossy다.
- `confirmed`: 같은 live 문서에서 `paragraphsWithMultipleStyleRefs=3`이 관측됐다. paragraph 단위 `styleRef` 하나만 대표값으로 쓰는 모델은 이미 실제 문서를 충분히 설명하지 못한다.
- `observed`: live `Csi` 배열은 거의 항상 짝수 길이이지만 숫자열이 monotonic하지 않았다. 따라서 단순 offset boundary 배열로 바로 읽기보다는 `offset + style code` 또는 `offset + flag` 쌍으로 보는 쪽이 더 유력하다.
- `observed`: live style histogram 상위값은 `117`, `156`, `59`, `62`였고, 제목/본문/불릿 문단이 서로 다른 `styleRef` 분포를 갖는 것은 분명하다.
- `observed`: 이 live 문서에서는 `childTypeHistogram`이 전부 `null`이었다. 현재 probe 범위에서는 table/image/page discriminator를 `vui.type`만으로 구분할 수 없었다.
- `observed`: control-like node는 여전히 주로 `Ooi=5`, `Hoi=1`, `idi=0` 패턴을 반복했고, table/image/page를 가리키는 명확한 별도 control signature는 이번 문서에서는 확보하지 못했다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-inline-runs.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860`가 정상 동작했고, 결과는 `tmp/discovery/02-read/inline-run-probe.json`에 저장했다.
- `confirmed`: 같은 live 문서에서 `nodesWithResolvedRuns=56`, `nodesWithMonotonicOffsets=56`, `nodesWithOutOfRangeOffsets=0`, `uniqueCharStyleCodeCount=103`이 관측됐다. 즉, sampled text node 전부에서 `Csi`를 ordered `(offset, charStyleCode)` pair로 보존할 수 있었다.
- `confirmed`: `Csi`의 오른쪽 값은 sampled 문서 전 범위에서 `Ivr.Y5n.n4n[code]` char-style entry로 resolve됐다. `Ivr.$5n.n4n[code]`는 일부 low index에서 우연히 존재할 수 있지만, inline run 해석의 1순위는 char-style code로 보는 쪽이 더 강하다.
- `observed`: `styleRef`는 paragraph/node의 base char-style과 para-style(`Ivr.Y5n/$5n[styleRef]`)를 제공하고, `Csi`는 node 내부 inline run의 char-style override를 제공하는 shape가 더 잘 맞는다.
- `observed`: sampled 문서에서는 `Csi` 첫 pair가 거의 항상 `start=0`이었고, 예외적 terminal sentinel은 `start===textLength` 형태로만 나타났다.
- `observed`: live inline-run histogram에서 `code 38/64/55/60`과 `199/203/215` 같은 char-style code가 반복 사용됐다. 동일 문단 `styleRef` 안에서도 더 세분화된 phrase-level run 분리가 이미 존재한다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-style-semantics.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860`가 정상 동작했고, 결과는 `tmp/discovery/02-read/style-semantics-probe.json`에 저장했다.
- `confirmed`: sampled 문서의 char-style table 전체(`239` entries)에서 `wTi === bTi.Msi`가 전 entry에서 성립했고, `vTi !== 0`인 entry는 하나도 없었다. 따라서 현재 문서의 static style table만으로는 bold/italic 의미를 확정할 수 없다.
- `confirmed`: sampled 문서의 para-style table 전체(`162` entries)에서 `Xli` histogram은 `1,2,3,4,5,6,7,8,10,11,22,25`를 포함했다. 따라서 `Xli`를 단순 left/center/right/justify enum으로 바로 해석하는 것은 근거가 약하다.
- `observed`: para-style의 별도 `wTi.Msi` 분포는 `2,7,0,1,5`였고, char-style `wTi`와 동일 축의 값처럼 보이지만 현재 단계에서는 의미명까지 연결할 증거가 없다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-caret-toolbar.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860`와 `npx tsx examples/discovery/02-read/sample-caret-toolbar-grid.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860`가 정상 동작했고, 결과는 `tmp/discovery/02-read/caret-toolbar-probe.json`, `tmp/discovery/02-read/caret-toolbar-grid.json`에 저장했다.
- `confirmed`: grid artifact의 comparable sample `35/35`에서 toolbar `lineSpacing` 값과 `AMe.Eni.sdi.Msi -> Ivr.$5n.n4n[styleRef].FNi / 100`가 일치했다. 현재 caret 기준 줄간격 read path는 para-style entry의 `FNi`로 보는 쪽이 강하다.
- `observed`: 같은 grid artifact에서 comparable sample `21/35`만 `AMe.Kqi === toolbar fontSize * 100`를 만족했다. 일치하지 않은 케이스는 주로 control-like/gibberish node였으므로, `Kqi`는 일반 text caret에서는 font-size candidate지만 control node에서는 신뢰도가 떨어진다.
- `confirmed`: grid artifact에서 toolbar `activeAlign`이 `both`와 `center`로 바뀌는 동안 `caret.Zys`, `caret.$ys`, `caret.Jys.Xli`는 전 sample에서 모두 `2`로 고정돼 있었다. 따라서 이 top-level caret field들은 direct alignment enum이 아니다.
- `confirmed`: 같은 grid artifact에서 toolbar `activeAlign=both`인 sample의 para-style `Xli`는 `1`, `2`, `5`, `22`까지 분산됐고, `activeAlign=center` sample은 `Xli=10`으로 모였다. `Xli`는 paragraph class/style discriminator일 가능성은 있지만, 단독 direct alignment enum으로 쓰기에는 반례가 충분하다.
- `confirmed`: `hwpCaret.AMe.Cni.pos/type`가 live current caret position을 제공했고, `node.Csi` pair 가운데 `start <= Cni.pos`인 마지막 pair를 선택하면 current inline run을 복구할 수 있었다.
- `confirmed`: 갱신된 `caret-toolbar-grid.json`의 text-like sample `33`개 기준으로 toolbar `fontName`은 base `styleRef -> Y5n[styleRef]`와 `1/33`만 일치했지만, current run `Csi + Cni.pos -> Y5n[charStyleCode]`와는 `32/33`이 일치했다.
- `confirmed`: 같은 sample 집합에서 toolbar `fontSize`는 base char-style과 `1/33`만 일치했고, current run char-style과는 `30/33`이 일치했다. 현재 text formatting read는 paragraph base style보다 active run style을 우선해야 한다.
- `observed`: 현재 sample에서 toolbar `fontColor=#000000`인데 `Y5n[styleRef]`와 current run `Y5n[charStyleCode]`의 `oqt`는 계속 `#c0c0c0`였다. color는 `oqt` 외의 다른 char-style field이거나 별도 live state carrier일 가능성이 있다.
- `failed`: 현 문서의 static style table만으로는 bold/italic/alignment 의미를 최종 확정할 수 없었다. 추가로 필요한 것은 bold/italic/alignment가 실제 포함된 샘플 문서 또는 live mutation 전후 diff probe다.
- `next`: `Csi`를 run offset으로 해석하고, `Y5n.wTi`, `Y5n.bTi.Msi`, alignment live state가 실제 어느 caret/layout field에 저장되는지 더 복구해야 export/write downstream이 안정화된다.
- `next`: current run 기준 color carrier를 찾아야 한다. 현재 `oqt`는 toolbar color와 맞지 않는다.
- `next`: 표나 이미지가 실제 포함된 샘플 문서에서 같은 probe를 다시 돌려 `childType`, control node pattern, paragraph mixing 여부를 비교해야 한다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-read-runs.ts --port 9223 --target-id 80563C4B8F102EF9494801E3A62C3C83`, `npx tsx examples/discovery/02-read/probe-inline-runs.ts --port 9223 --target-id 80563C4B8F102EF9494801E3A62C3C83 12`, `npx tsx examples/discovery/02-read/probe-style-semantics.ts --port 9223 --target-id 80563C4B8F102EF9494801E3A62C3C83`가 정상 동작했고, 결과는 `tmp/discovery/02-read/read-run-probe-doc2.json`, `inline-run-probe-doc2.json`, `style-semantics-probe-doc2.json`에 저장했다.
- `confirmed`: 두 번째 live 문서에서는 `paragraphCount=176`, `textNodeCount=319`, `controlNodeCount=39`, `paragraphsWithMultipleStyleRefs=58`, `nodesWithMultipleRunCandidates=126`이 관측됐다. 첫 문서보다 multi-style paragraph와 control node가 훨씬 많아 richer structure probe용 샘플로 더 적합하다.
- `observed`: 기존 `childType=null` 관측은 direct `node.vui?.type` 경로를 읽은 결과였다. probe를 `node.Loi[0].vui.type`로 교정한 뒤에는 두 문서 모두 full scan 전 범위에서 `childType=1`, `childPos=-1`만 관측됐다.
- `confirmed`: 새 probe `npx tsx examples/discovery/02-read/probe-structure-discriminators.ts --port 9223 --target-id <doc>`를 두 문서에 실행했고, 결과는 `tmp/discovery/02-read/structure-discriminator-probe-doc1.json`, `structure-discriminator-probe-doc2.json`에 저장했다.
- `confirmed`: 두 문서 모두 sampled node 전체에서 `Loi.length=1`, `Loi[0].vui.keys=["pos","type"]`, `type=1`, `pos=-1`이었다. `Loi[0].vui`는 exact child metadata path지만 현재 표/이미지/페이지를 직접 구분하는 값은 아직 제공하지 않는다.
- `confirmed`: control signature histogram에서 `Ooi=5`, `koi=2`, `Hoi=1`, `Moi=1`, `idi=0`, `ndi=false`, `textLength=6` 패턴이 첫 문서 `14/15`, 두 번째 문서 `28/39`로 지배적이었다. 이 짧은 control token은 current strongest embedded-object anchor candidate다.
- `observed`: 두 번째 문서의 여러 control sample은 `prevText`가 소제목/주석이고 `nextText`가 `(단위: 천명, %, %p)` caption으로 이어졌다. 특히 이 bucket의 payload는 반복적으로 `Noi=9`, `Jci=2048`, `adi.Cci=[]`, `hdi/udi=0-filled`였으므로 chart/table/image 중 하나의 공통 anchor일 가능성이 높다.
- `failed`: `Loi[0].vui.type/pos`, dominant control signature, `Noi/Jci/qci/Zci` 조합만으로는 table/image/chart/page를 exact discriminator로 분리하지 못했다. UI state나 sibling root와 교차 확인하는 다음 probe가 필요하다.
- `confirmed`: 두 번째 문서의 static style table도 `charEntriesWhereWtiEqualsBTiMsi=447/447`, `charEntriesWithNonZeroVti=0/447`였다. 첫 문서보다 `wTi=3` 분포와 `paraEntriesWithXliOverSeven=73`처럼 범위는 넓어졌지만, bold/italic/alignment 의미를 static table만으로 확정할 수 없다는 결론은 유지된다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-image-registry.ts --port 9223 --target-id 80563C4B8F102EF9494801E3A62C3C83`를 추가/실행했고, 결과는 `tmp/discovery/02-read/image-registry-probe-doc2.json`에 저장했다.
- `confirmed`: 두 번째 문서에서 `HwpApp.document.Ivr.u6n.U4n`는 `18`개 entry를 가진 image registry였고, 각 entry는 최소 `Qli`, `Xli`, `FFi`, `UFi`를 가졌다.
- `confirmed`: 같은 문서에서 `HwpApp.cache.images[FFi]`는 실제 `HTMLImageElement`를 반환했고, `src/currentSrc`, `naturalWidth`, `naturalHeight`를 직접 읽을 수 있었다. 예를 들어 `U4n[0].FFi === "01DCBF443E3726000000F808.jpg"`는 blob URL과 `1181x1181` intrinsic size로 resolve됐다.
- `observed`: image-bearing 문서에서도 `Loi[0].vui.type`는 계속 `1`만 관측됐다. image discriminator를 `vui.type`로 잡는 접근은 그대로 실패다.
- `confirmed`: image placeholder 후보 control node는 `Aoi` 안에 ASCII-like 4-byte token과 small id를 반복해서 담고 있었다. live sample에서 node `0` (`Ooi=25`, `koi=8`, `Hoi=4`)는 `dces`, `dloc`, `pngp`, `dhgp`, ` lbt` token과 id `3..8`을 담았고, 이 id는 `Ivr.u6n.U4n[*].Qli`에 직접 매칭됐다.
- `confirmed`: 현재 strongest image read path는 `text-chain control -> parsed placeholder refs -> Ivr.u6n.U4n image registry -> HwpApp.cache.images payload`의 2-stage 구조다.
- `observed`: 일부 control placeholder는 image-like token을 갖지만 `U4n`에 바로 매칭되지 않았다. image registry는 확보됐지만 non-U4n graphic/table registry는 아직 남아 있다.
- `confirmed`: live click probe `tmp/discovery/02-read/select-image-grid.ts`를 두 번째 문서에 실행했고, 결과는 `tmp/discovery/02-read/select-image-grid.json`에 저장했다. xOffset `520+` 영역의 object-like selection은 `nodeId=01DCBF443E24F3E00000C0E3`, `01DCBF443E24F3EA0000C0E4` 같은 control node로 수렴했지만, 이 node들의 `Aoi`는 `U4n` image id가 아니라 `19..24,26` 같은 다른 control id를 담고 있었다.
- `confirmed`: 새 graph probe `tmp/discovery/02-read/search-image-specific-graph.ts` 결과 `HwpApp.document.Ivr.j5n.n4n[*]`가 `Qli`, `Xli`, `qli`를 가진 object registry로 확인됐다. 이 registry는 `Qli=1,2,9,11,17,18`뿐 아니라 click으로 드러난 `19..24,26`도 보유한다.
- `confirmed`: 따라서 current strongest placement/object path는 `text-chain control node.Aoi -> object id(Qli) -> Ivr.j5n.n4n[*]`이고, 이 `Qli`가 동시에 `Ivr.u6n.U4n[*].Qli`에도 존재하면 해당 object는 `cache.images[FFi]`로 resolve되는 image payload를 가진다.
- `observed`: `U4n`에 존재하지만 `Aoi` placeholder로 직접 보이지 않는 id `1,2,9,11,17,18`도 모두 `j5n.n4n[*]` object entry는 갖고 있었다. 현재 문서 기준으로는 이들이 direct text-chain anchor가 아니라 auxiliary graphic/image object이거나 다른 상위 object에 매달린 sub-resource일 가능성이 높다.
- `observed`: object selection hit에서 반복된 `nodeId=01DCBF443E24F3E00000C0E3`는 `Aoi`에 `19,21,22,23`을 담았고, `nodeId=01DCBF443E24F3EA0000C0E4`는 `24`를 담았다. 이 id들은 `U4n`에는 없고 `j5n.n4n`에만 존재하므로, 현재 문서의 object/table-like control은 image payload 없이 object registry만 가지는 케이스가 분명히 존재한다.
- `confirmed`: live 재검증에서 `17,18`은 text-chain `Aoi/hdi/udi/adi.Cci`에는 나타나지 않았고, sampled hit는 전부 `Csi` char-style code 충돌이었다. 따라서 현재 문서에서 `17,18`을 direct object/image anchor로 해석하면 안 된다.
- `confirmed`: fallback `pageReadStructureFromTextChain()`는 이제 direct image anchor `12`개를 구조 node로 내보내고, 동시에 `U4n`에만 존재하는 unanchored asset id `1,2,9,11,17,18`을 warning으로 노출한다.
- `confirmed`: 새 probe `npx tsx examples/discovery/02-read/probe-image-asset-anchors.ts --port 9223 --target-id 5F78070AC295BA2AC39D866662823F43 2000 200000 24`를 추가/실행했고, 결과는 `tmp/discovery/02-read/image-asset-anchor-probe-doc2.json`에 저장했다.
- `confirmed`: 위 probe는 `HwpApp.document`, `HwpApp.cache`, `HwpApp` 루트 전체에서 exact object identity와 exact string ref를 다시 스캔했고, `visitedGraphNodes=131358`에서 종료됐다.
- `confirmed`: unanchored image asset `1,2,9,11,17,18`은 전부 `document.Ivr.u6n.U4n[index]`, `document.Ivr.j5n.n4n[index]`, `document.Ivr.u6n.U4n[index].FFi`, `cache.images[FFi]`, `document.Ivr.j5n.n4n[index].qli` 외의 추가 ref가 한 번도 나타나지 않았다.
- `confirmed`: 같은 probe에서 각 asset의 실제 `HTMLImageElement` identity도 추적했지만 `cache.images[FFi]` 외의 추가 ref는 없었다. 즉 renderer나 다른 상위 object가 cached image element 자체를 따로 잡고 있는 흔적도 현재 문서에서는 발견되지 않았다.
- `observed`: 이 unanchored asset들도 실제 payload는 정상이며 크기는 각각 `1181x1181`, `334x133`, `789x250`, `442x202`, `479x226`, `627x346`으로 읽힌다. 따라서 "깨진 이미지"가 아니라 "배치 anchor가 없는 loaded asset" 쪽 해석이 더 강하다.
- `observed`: 따라서 현재 strongest interpretation은 이 6개가 direct placed image가 아니라 registry/cache에만 남은 asset 또는 다른 object 내부 구현용 sub-resource라는 쪽이다. 현재 문서의 live graph scan만으로는 별도 placement parent를 찾지 못했다.
- `observed`: 이후 같은 live target을 다시 스캔했을 때 text-chain direct anchor 집합이 바뀌었다. 최신 `probe-image-asset-anchors` 결과 anchored ids는 `1,3,4,10,12,13,14,15,16`, unanchored ids는 `2,5,6,7,8,9,11,17,18`이었다.
- `confirmed`: `imageId=1` anchor node (`nodeIndex=626`, `nodeId=01DCBF443E24F3540000C0D4`)는 `Aoi=[11,27680,29794,1,...]` 형태의 10-word placeholder를 가졌지만 decoded text에 garbage Hangul-like 글자가 섞여 기존 `looksLikeControlText()` gate를 통과하지 못했다.
- `confirmed`: 따라서 `pageReadStructureFromTextChain()`는 image ref scan을 control-text heuristic보다 먼저 수행하도록 수정했다. 최신 live fallback 결과는 image block `9`개였고 ids는 object node 기준 `01DCBF443E273ECA0000F0D6`, `01DCBF443E273F6A0000F0D8`, `01DCBF443E273FB00000F0D9`, `01DCBF443E2741400000F0DF`, `01DCBF443E2741CC0000F0E1`, `01DCBF443E2742080000F0E2`, `01DCBF443E2742440000F0E3`, `01DCBF443E27428A0000F0E4`, `01DCBF443E2743020000F0E5`였다.
- `observed`: 최신 live snapshot에서는 예전에 direct anchor로 관측됐던 `5,6,7,8`이 text-chain `Aoi/hdi/udi/adi.Cci` 전체에서 더 이상 나타나지 않았다. 현재는 runtime state drift 또는 placeholder grammar 변화 후보로 보류한다.
- `confirmed`: fallback public formatting도 승격됐다. `pageReadStructureFromTextChain()`의 paragraph/run style은 이제 `runCharStyle.cUt` low bits로 `bold/italic`, `runCharStyle.$Xt` BGR-packed int로 visible `color`, `paraStyle.cUt`로 `alignment`, `paraStyle.FNi`로 `lineSpacing`을 직접 채운다.
- `confirmed`: latest live fallback paragraph sample `01DCBF443E250CD60000C344`에서도 run `textStyle`에 `bold`, `italic`, `color`, `fontName`, `fontSize`가 함께 노출됐다. leading body-like runs는 `bold=false`, `color=#000000`, `한양중고딕 10/11pt`, trailing heading-like runs는 `bold=true`, `휴먼명조 12/15pt`로 read-back됐다.
- `confirmed`: 새 probe `npx tsx examples/discovery/02-read/sample-selection-state-grid.ts --port 9223 --target-id <doc>`를 두 문서에 실행했고, 결과는 `tmp/discovery/02-read/selection-state-grid-doc1.json`, `selection-state-grid-doc2.json`에 저장했다.
- `confirmed`: 첫 번째 plain-text 문서 baseline에서는 `enabledCommandNames=[]`, `objectSignals=[]`, `tableSignals=[]`였고, sampled `42`개 click 전부에서도 object/table signal이 한 번도 켜지지 않았다. probe는 non-object/non-table context에서 false positive를 만들지 않았다.
- `confirmed`: 두 번째 문서 baseline에서는 이미 `enabledCommandNames=["modify_object_properties"]`였고 caret node는 control-like `nodeId=01DCBF443E24F5840000C111`, `Ooi=9`, `Jci=2099200`, `Noi=17`이었다.
- `confirmed`: 두 번째 문서 grid sampled `42`개 중 `21`개에서 `modify_object_properties`가 반복적으로 enabled 되었고, 이 hit는 전부 같은 control-like node (`Ooi=9`, `Jci=2099200`)에 고정됐다. 현재 strongest object-selection secondary signal은 `modify_object_properties` + current caret control signature의 결합이다.
- `observed`: 같은 grid에서 `e_insert_caption`은 추가로 켜지지 않았고, `c_insert_row_col_list`, `c_remove_row_col_list`, `dialog_edit_table`도 한 번도 enabled 되지 않았다. 현재 열린 두 문서만으로는 table-selection context를 재현하지 못했다.
- `observed`: 두 번째 문서에서 xOffset `520+` 영역은 object signal을 유지했고, `360-` 영역은 같은 control node를 가리키더라도 signal이 비활성화됐다. canvas hitbox와 object-selection UI state 사이에 별도 threshold가 존재하는 것으로 보인다.
- `next`: selection-state probe를 table cell이 확실한 live sample이나 write-side create-table 직후 상태와 결합해서 `c_insert_row_col_list` / `dialog_edit_table` enablement가 실제 table-selection exact proxy가 되는지 확인해야 한다.
- `confirmed`: `selectionStateProbe`는 이제 `getWidgetElementList()` wrapper snapshot과 별도로 direct DOM command census(`data-command` / `data-ui-value`)와 raw `enabledActionIds`를 함께 반환한다. `e_insert_caption` hidden submenu는 selection signal에서 제외했고, object-context는 다시 `modify_object_properties` 하나만 보조 라벨로 유지했다.
- `confirmed`: 새 one-shot example `npx tsx examples/discovery/02-read/probe-selection-state.ts --port 9223 --target-id <doc>`를 추가했고, clean snapshot은 `tmp/discovery/02-read/selection-state-probe-doc1.json`, `selection-state-probe-doc2.json`에 저장했다.
- `confirmed`: 현재 clean baseline 기준 첫 번째 문서는 `enabledCommandNames=[]`, `tableSelectionSignals=[]`, `objectSelectionSignals=[]`, `enabledActionIds=[35456]`였다. 두 번째 문서도 현재 caret 기준으로는 object/table signal이 비어 있었고, raw note만 dominant short control signature(`Ooi=5`, `Jci=2048`)를 남겼다. 즉 action bit `35456`만으로는 selection kind를 승격할 수 없다.
- `confirmed`: 새 follow-up `npx tsx examples/discovery/02-read/create-table-row-command-followup.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860 2 2 insert_lower_row`를 실행했고, 결과는 `tmp/discovery/02-read/create-table-row-command-followup-doc1.json`에 저장했다.
- `confirmed`: 위 follow-up에서 `createResult.ok=true`, `rowCommandResult.ok=true`였고, direct DOM command states는 `c_insert_row_col_list`, `c_remove_row_col_list`, `dialog_edit_table`가 모두 `disabled=false`로 유지됐다. visible widget만 보면 숨겨져 있지만, hidden command surface까지 포함하면 table context가 실제로 살아 있다.
- `confirmed`: 같은 follow-up의 `afterRunSelection`은 `tableSelectionSignals=["c_insert_row_col_list","c_remove_row_col_list","dialog_edit_table"]`, `objectSelectionSignals=["modify_object_properties"]`, `enabledActionIds=[35456,35474]`를 반환했다. 즉 table selection proxy는 `visible widget`이 아니라 `hidden-but-enabled table command census` 쪽이 더 강하다.
- `observed`: 같은 follow-up에서 `dialogClosed` heuristic은 계속 `false`였다. dialog title text visibility는 create-table success verification으로 신뢰하기 어렵고, command enablement나 trace/action evidence를 우선해야 한다.
- `observed`: `35474`는 row command follow-up 이후 함께 켜졌지만, exact table discriminator로는 아직 승격하지 않았다. 현재는 raw action evidence로만 보존하는 편이 안전하다.
- `confirmed`: 새 probe `src/hancom/discovery/02-read/tableContextStructureProbe.ts`와 follow-up `npx tsx examples/discovery/02-read/create-table-structure-join-followup.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860 2 2 insert_lower_row`를 추가/실행했고, 결과는 `tmp/discovery/02-read/create-table-structure-join-followup-doc1.json`에 저장했다.
- `confirmed`: 위 follow-up에서 `afterSelection.tableSelectionSignals=["c_insert_row_col_list","c_remove_row_col_list","dialog_edit_table"]`, `objectSelectionSignals=["modify_object_properties"]`가 다시 재현됐다. hidden table command surface proxy는 반복 재현 가능하다.
- `confirmed`: 같은 tick에서 branch delta를 비교하면 `HwpApp.document.Svr._ie.length`가 `5 -> 10`으로 증가했고, 상위 `HwpApp.document.Svr`에서도 child collection `_ie` 길이 변화가 직접 관측됐다. `_ie`는 table-context join용 strongest new branch candidate다.
- `observed`: 같은 follow-up에서 `HwpApp.document.Zvr.$bi`는 ownKeys가 `... qli, vie, _ie`에서 `... qli, cUt, gun, Tun, $9i, Lun, vqt, Mun`으로 크게 바뀌었고, scalar `j0i`, `Y0i`, `Q0i`, `mKi`, `qli`도 함께 변했다. `Zvr.$bi`는 table/object selection tick에 민감한 alternate root/state carrier 후보로 승격할 가치가 있다.
- `observed`: `HwpApp.document.Zvr.$bi.G0i`도 같은 tick에서 `Ooi`, `Noi`, `Xci`, `Jci` scalar와 `Aoi.length 34 -> 2` 변화가 관측됐다. 다만 이 값이 table 본체를 직접 뜻하는지, selection anchor만 뜻하는지는 아직 미확정이다.
- `observed`: `HwpApp.document.Svr.vie`에서도 scalar `yen` 변화가 보였지만, 현재 증거 강도는 `_ie` / `Zvr.$bi`보다 약하다.
- `next`: 다음 probe는 `Svr._ie[*]` element의 scalar/id field와 `Zvr.$bi.z0i` child branch를 같은 table tick에서 더 깊게 읽어, hidden table command surface와 실제 table-bearing runtime object를 직접 연결해야 한다.
- `confirmed`: 새 deep probe `src/hancom/discovery/02-read/tableContextDeepProbe.ts`와 follow-up `npx tsx examples/discovery/02-read/create-table-deep-structure-followup.ts --port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860 2 2 insert_lower_row`를 추가/실행했고, 결과는 `tmp/discovery/02-read/create-table-deep-structure-followup-doc1.json`에 저장했다.
- `confirmed`: 같은 deep follow-up에서 table signal은 다시 `["c_insert_row_col_list","c_remove_row_col_list","dialog_edit_table"]`로 재현됐다. 즉 deep probe를 붙여도 table-selection proxy는 흔들리지 않았다.
- `observed`: `Svr._ie`는 run마다 길이가 바뀌지만, entry shape 자체는 거의 동일했다. 새로 붙은 entry도 `ubi=0`, `cZi=0`, `dZi=false`, `Y9i=1`, `v8i=0`를 그대로 유지한다.
- `observed`: 다만 새로 붙은 `Svr._ie[9]`의 child `hie`는 기존 entry와 달랐다. baseline entry `0`은 `K7i=true`, `zXi=1`, `tyi=0`이었고, 새 entry `9`는 `K7i=false`, `zXi=10`, `tyi=9`였다. `_ie` 자체보다 `_ie[*].hie`가 table tick discriminator 후보로 더 강하다.
- `confirmed`: 같은 follow-up에서 `Zvr.$bi.z0i`는 table tick 이전에는 비어 있었지만, 이후에는 `qli="0000019D387683CD15790156"`, `cUt=136979216`, `iin=47624`, `SXt=2568`, `gnn=47624`, `Tnn=2568`, `Snn=28`, `Dnn=4`, `Zci=2019152342` 같은 scalar를 갖는 object로 나타났다.
- `observed`: `Zvr.$bi.z0i` child 중 `Nji`는 direct node-like object였고 `Ooi=5`, `koi=2`, `Noi=9`, `Hoi=1`, `Moi=1`을 가졌다. 이는 현재 text-chain에서 반복 관측한 dominant short control signature와 같은 bucket이다.
- `observed`: `Zvr.$bi.gun`은 길이 `1` array로 생겼고, 그 단일 entry shape는 `Svr._ie[*]`와 거의 동일했다. 반면 `Lun`, `vqt`, `Mun`은 길이 `2` 숫자 배열로만 나타났다.
- `next`: 다음 probe는 `Svr._ie[*].hie.{K7i,zXi,tyi}`와 `Zvr.$bi.z0i.{qli,cUt,iin,SXt,Snn,Dnn}`를 text-chain node id/registry id와 cross-join 해서, table-bearing object가 실제 어느 control node나 registry entry에 대응하는지 확인해야 한다.
- `confirmed`: 새 probe `npx tsx examples/discovery/02-read/probe-format-state.ts --port 9223 --target-id <doc>`와 보조 grid/point probe(`sample-format-state-grid.ts`, `probe-format-state-at.ts`)를 추가했고, 결과는 `tmp/discovery/02-read/format-state-probe-doc1.json`, `format-state-probe-doc2.json`, `format-state-grid-doc1-top.json`, `format-state-grid-doc1-mini.json`, `format-state-grid-doc2-mini.json` 등에 저장했다.
- `confirmed`: toolbar command surface는 `font_name`, `font_size`, `bold`, `italic`, `underline`, `font_color`, `p_align`, `p_line_spacing`, `e_style_item`까지 direct DOM에서 재현 가능했다. toolbar DOM은 current caret/selection의 projection이므로 exact document read path가 아니라 verification label로만 쓰는 편이 안전하다.
- `confirmed`: current-caret upstream cache 후보로 `HwpApp.hwpCaret.CSr.L3e`와 `HwpApp.hwpCaret.CSr.M3e`를 확보했다. doc1 sampled text에서 `L3e.sge/hge.kHt.16384/16385`는 toolbar `fontName="바탕"`와 일치했고, `M3e.Qge/Xge.kHt.16390` 및 `M3e.nge.FNi`는 toolbar line-spacing과 일치했다.
- `observed`: `CSr.L3e.nge`와 `CSr.M3e.nge`는 current char/para style cache처럼 보이지만, doc1 text sample에서 `toolbar.bold=true`일 때도 `L3e.nge.wTi=0`, `L3e.nge.vTi=0`, `L3e.nge.Xli=0`이 유지됐다. 따라서 bold/italic 의미를 `CSr.L3e.nge.wTi/vTi`에 직접 고정하면 안 된다.
- `confirmed`: doc1 mini grid 기준 toolbar `bold=true` sample은 `currentRun.charStyleCode=73` (`text="내 통과"`)와 `64` (`text=" 지자체, LH와 SH, GH"`)에서 관측됐고, plain sample은 `38`, `70`, `225`, `62`였다. current bold candidate는 single boolean field보다 `charStyleCode -> Y5n entry identity` 쪽이 더 강하다.
- `observed`: doc1 style-code inspect 기준 `Y5n[64]`, `Y5n[73]`, `Y5n[70]`, `Y5n[38]`, `Y5n[225]`는 모두 `font="바탕"`, `vTi=0`, `wTi===bTi.Msi===1`을 공유했다. 차이는 주로 `dTi[0]`, `Xli`, `mTi`, `Qli/qli`에 있어, bold는 아직 미해석 char-style field 조합 또는 style code identity 자체에 묶여 있을 가능성이 높다.
- `observed`: doc2 sampled text에서는 `charStyleCode 119/159/178/180`이 `wTi===bTi.Msi===3` 그룹으로 나타났고, sampled para-style `277/400/402/403/404`는 모두 `Xli=1`, `FNi=160`, `wTi.Msi=3`, `HNi=1000`, `PNi=400`을 공유했다. 현재 확보한 doc2 sample만으로는 alignment variance를 다시 재현하지 못했다.
- `observed`: format-state probe를 다시 돌린 현재 selection에서도 toolbar `fontColor=#000000`와 `CSr.L3e.nge.oqt=12632256 (#c0c0c0)` 불일치는 유지됐다. visible text color carrier는 `oqt` 외의 다른 live state 또는 transformed payload일 가능성이 높다.
- `next`: 다음 style-semantics 단계는 `toolbar.bold on/off` sample을 더 안정적으로 고정한 뒤 `charStyleCode` diff와 toolbar update source를 같이 추적하는 것이다. 특히 `Y5n[64/73]` vs `Y5n[38/70/225]`에서 `dTi/pTi/mTi/Xli` 중 어느 축이 bold 의미를 담당하는지 더 좁혀야 한다.
- `confirmed`: 별도 worker Chrome(`npm run example:chrome-workers -- launch --base-port 9333 --worker-count 1`)를 띄우고 `mark-ready` 후, port `9333`의 isolated profile에서도 같은 editor probe를 재현했다. target 목록은 `tmp/discovery/chrome-workers/registry.json` 및 `npx tsx examples/list-targets.ts --port 9333`로 확인했다.
- `confirmed`: worker doc1 wide grid(`tmp/discovery/02-read/format-state-grid-worker-doc1-wide.json`)에서 `xOffset=580,yOffset=140` sample은 실제 text `보도자료`를 가리켰고 toolbar `activeAlign=center`, `fontName=함초롬바탕`, `fontSize=14.0`, `charStyleCode=40`, `styleRef=37`를 반환했다. isolated worker에서도 center-aligned text sample이 재현 가능하다.
- `observed`: 같은 worker sample의 para-style inspect 기준 `Ivr.$5n[37]`은 `Xli=4`, `FNi=160`, `wTi.Msi=2`였다. 한편 기존 main-session centered/both sample과 비교하면 `Xli` 하나만 direct alignment enum으로 쓰기에는 여전히 근거가 약하다.
- `confirmed`: worker doc1 wide grid에서 `xOffset=120,yOffset=200/260/320/380` sample은 toolbar `bold=true`, `activeAlign=center`, `fontName=HY헤드라인M`, `fontSize=6.0`, `charStyleCode=36`, `styleRef=3`를 반복 반환했다.
- `observed`: worker에서 잡힌 `charStyleCode=36` entry는 `Y5n[36].wTi===bTi.Msi===2`, `vTi=0`, `font=HY헤드라인M 6pt`, `Xli=1`이었다. same-worker plain heading-like entries `Y5n[52]`도 `wTi===bTi.Msi===2`를 공유하므로, worker sample 역시 bold 의미를 `wTi` 단독으로 확정해주지는 않았다.
- `confirmed`: probe를 확장해 `currentRun` char-style/para-style 핵심 필드(`$Xt`, `cUt`, `dTi.0`, `bTi.Msi`, `LNi`)를 direct sample map으로 노출했다. 새 artifact는 `tmp/discovery/02-read/format-state-grid-worker-doc1-color-rerun.json`, `format-state-at-worker-doc1-blue-bold.json`이다.
- `observed`: worker rerun에서 toolbar `fontColor=#ffffff` sample(`charStyleCode=39`)은 `runCharStyle.$Xt=16777215`와 맞았고, toolbar `fontColor=#0000ff` sample(`charStyleCode=77`)은 `runCharStyle.$Xt=16711680 (0xff0000)`와 맞았다. 현재 strongest color candidate는 `Y5n[charStyleCode].$Xt`이며, visible color는 RGB가 아니라 BGR-packed integer일 가능성이 높다.
- `observed`: 같은 rerun에서 toolbar `bold=true` sample들은 `charStyleCode 36/44/48/77`로 넓어졌고, 이들의 `runCharStyle.cUt`는 `1006895106` 또는 `1006895110`이었다. 반면 sampled plain entries `39/40/52/71`은 `1006895104`였다. current bold candidate는 `wTi/vTi`보다 `cUt` low-bit group 쪽이 더 강하다.
- `observed`: worker rerun에서 toolbar `activeAlign=right` sample(`styleRef=35`)과 toolbar `activeAlign=center` sample(`styleRef=3`)가 모두 para-style `Xli=1`을 공유했다. `Xli`를 direct alignment enum으로 해석하는 가설은 더 약해졌다.
- `observed`: 같은 rerun에서 `caretCharStateSampleMap`은 자주 stale/null이었지만, `runCharStyleSampleMap`은 toolbar font/color/bold 후보와 더 안정적으로 맞았다. current-caret formatting read는 `CSr.L3e/M3e`보다 `currentRun.charStyleCode -> Y5n entry`를 우선 검토하는 편이 안전하다.
- `confirmed`: 새 probe `src/hancom/discovery/02-read/tableContextJoinProbe.ts`와 follow-up `npx tsx examples/discovery/02-read/create-table-join-followup.ts --port 9333 --target-id <doc> 2 2 insert_lower_row`를 추가했고, 결과는 `tmp/discovery/02-read/create-table-join-followup-worker-doc1.json`, `create-table-join-followup-worker-doc2.json`에 저장했다.
- `confirmed`: worker doc1 follow-up에서 `HwpApp.document.Zvr.$bi._Vi.z0i`, `.Oji`, `.Oji.Oji`, `.Oji.Oji.Oji`의 `qli/cUt`는 각각 `HwpApp.document.Ivr.o6n.n4n[9..12].zli`와 exact join됐다.
- `confirmed`: 같은 doc1 run에서 `HwpApp.document.Zvr.$bi._Vi.z0i.Oji.Oji.Nji`, `.Oji.Nji`, `.Oji.Nji.tdi`, `.kji.kji.Nji`의 `qli`는 `HwpApp.document.Svr.G0i`와 `HwpApp.document.Zvr.$bi.G0i` control node id로 exact join됐다.
- `confirmed`: worker doc1 run에서 `HwpApp.document.Zvr.$bi._ie[*].V9i.sVi`는 `Ivr.o6n.n4n[4].zli`, `_ie[*].hie.abi`는 `Ivr.o6n.n4n[3].zli`와 exact join됐다. `_ie`도 table-context registry를 직접 참조한다.
- `confirmed`: worker doc2 run에서도 같은 패턴이 재현됐다. `HwpApp.document.Zvr.$bi._Vi.z0i`, `.Oji`, `.Oji.Oji`, `.Oji.Oji.Oji`, `.kji`, `.kji.kji`, `.kji.kji.kji`가 각각 `Ivr.o6n.n4n[*].zli`와 exact join됐고, nested node-like child 일부는 `Svr.G0i` 또는 `Svr._Vi.G0i` node id로 이어졌다.
- `confirmed`: worker doc2 run에서는 `HwpApp.document.Zvr.$bi._ie[*].hie.abi`와 `.hie.Q7i`가 각각 `Ivr.o6n.n4n[3].zli`, `Ivr.o6n.n4n[6].zli`와 exact join됐다. doc1보다 `_ie.hie` 내부 id carrier가 더 풍부하게 드러났다.
- `observed`: 두 worker run 모두 초기 `Svr._ie[*].hie` histogram은 `[{K7i:true,zXi:1,tyi:0},{false,2,1},{false,3,2},{false,4,3},{false,5,4}]`로 안정적이었다. 다만 이후 `Tab` walk evidence를 합치면 `zXi/tyi`를 active current-cell ordinal로 직접 해석하는 것은 틀리다.
- `observed`: doc2 follow-up에서는 exact table join이 살아 있었는데도 `afterSelection.tableSelectionSignals=[]`였다. hidden command surface proxy는 useful하지만, table verification priority는 widget signal보다 `Zvr.$bi._Vi.z0i` / `_ie.hie` structural join 쪽이 높다.
- `confirmed`: 새 worker follow-up `npx tsx examples/discovery/02-read/create-table-tab-walk.ts --port 9333 --target-id <doc> <rows> <cols> <tabSteps>`를 추가했고, 결과는 `tmp/discovery/02-read/create-table-tab-walk-worker-doc1-3x3-8tabs-v2.json`, `create-table-tab-walk-worker-doc1-2x2-6tabs.json`, `create-table-tab-walk-worker-doc1-3x3-10tabs.json`, `create-table-tab-walk-worker-doc2-3x3-10tabs.json`에 저장했다.
- `confirmed`: raw CDP `Input.dispatchKeyEvent(Tab)`는 실제 editor 내부 caret traversal을 일으켰다. doc1 `3x3` run에서 `caretNodeId`는 `...1606 -> ...1608 -> ...160A ... -> ...1616`처럼 step마다 변했고, `tableSelectionSignals`도 계속 유지됐다.
- `observed`: doc1에서는 table 내부 traversal 동안 `_ie.hie`가 고정 길이 `5`로 유지되고 `zXi/tyi`도 `1..5 / 0..4`에서 변하지 않았다. 즉 `_ie.hie.{zXi,tyi}`를 active-cell ordinal로 직접 해석하면 틀린다.
- `observed`: 같은 doc1 run에서 `Tab` 누적 횟수가 `rows * cols` 경계를 넘는 즉시 `_ie`가 `5 -> 48`로 급증했다. `2x2`는 `step4`, `3x3`는 `step9`에서 재현됐고, caret node id prefix도 함께 바뀌었다. strongest interpretation은 "마지막 셀 통과 후 broader table-context mode로 전이"다.
- `observed`: doc2는 같은 `3x3` 시퀀스에서도 다른 profile을 보였다. `step0`은 `_ie=5`, 첫 `Tab` 이후는 `_ie=10`으로 바뀌지만 `step9/10`에서도 `48` 확장은 일어나지 않았다. `_ie` cardinality는 문서별 baseline과 traversal phase에 의존하며, direct cell count mirror는 아니다.
- `observed`: `z0i` exact registry join은 traversal 동안 유지되지만 `Ivr.o6n.n4n[*].zli` target set은 step마다 일부 회전했다. 즉 `z0i` branch는 table-context registry anchor로는 강하지만, 현재 요약 수준만으로 row/col/current-cell을 직접 복원하기엔 아직 부족하다.
- `confirmed`: `create-table-tab-walk`를 forward/reverse capture로 확장했고, `transitionFromPrevious.{ieCountDelta, addedRegistryKeys, removedRegistryKeys}`와 `z0iCarrierSummaries[]`를 함께 저장하도록 바꿨다. 새 artifact는 `tmp/discovery/02-read/create-table-tab-walk-worker-doc1-2x2-4forward-4reverse.json`, `create-table-tab-walk-worker-doc2-3x3-9forward-4reverse.json`, `create-table-tab-walk-worker9335-doc1-3x3-9forward-4reverse.json`이다.
- `observed`: doc1 `2x2` reverse walk에서는 boundary burst(`5 -> 48`)가 다시 재현됐지만, 직후 `Shift+Tab`은 caret와 registry set을 한 번 바꾸는 데 그쳤고 `_ie`는 `48`으로 유지됐다. `Shift+Tab`만으로 pre-boundary `5`-entry phase로 즉시 되돌아가지는 않는다.
- `observed`: doc2 `3x3` reverse walk에서는 `_ie=5`가 유지된 채 `caretNodeId`와 `z0i -> o6n.zli` target set이 backward order로 회전했다. reverse replay는 가능하지만 `_ie` cardinality transition을 동반하지 않는다.
- `observed`: fresh worker(`:9335`)의 doc1 `3x3` rerun은 `step7+`에서 control-anchor node id로 진입했지만 `_ie`는 끝까지 `5`를 유지했고 `48` burst가 재현되지 않았다. doc1의 large `_ie` boundary phase는 table dimensions만으로 결정되지 않고 selection history 또는 preceding context에 민감할 가능성이 높다.
- `next`: 다음 단계는 `Ivr.o6n.n4n[*].zli` entry class를 table semantic registry로 해석하는 것이다. 특히 `z0i` chain 순서와 `_ie.hie.{abi,Q7i}`가 row/col/cell/order 중 무엇을 represent하는지 좁혀야 한다.
- `next`: 남은 재현성 blocker는 doc1 `48` burst의 trigger다. 현재는 `Shift+Tab` reverse만으로는 되감기되지 않으므로, clean-state protocol 또는 arrow-key/control-anchor branch diff를 추가로 모아야 한다.
- `confirmed`: 새 probe `examples/discovery/02-read/probe-paragraph-styles.ts` / `src/hancom/discovery/02-read/paragraphStyleProbe.ts`를 추가했고, worker doc1/doc2 artifact를 `tmp/discovery/02-read/paragraph-style-probe-worker-doc1.json`, `paragraph-style-probe-worker-doc2.json`에 저장했다.
- `confirmed`: 새 paragraph-style probe는 text-chain paragraph마다 `paraStyleRefs[]`, `paragraphStyleConsistent`, resolved `lineSpacing`, raw `cUt/Xli/LNi/wTi.Msi`, inline run char-style 요약(`fontName`, `fontSize`, `$Xt`, `cUt`)을 한 snapshot으로 묶는다. 이로써 `normalized-snapshot` subtask에서 요구한 paragraph anchor + style raw/candidate bundle이 repro 가능한 artifact로 생겼다.
- `confirmed`: paragraph line spacing의 strongest exact path는 그대로 `Ivr.$5n.n4n[styleRef].FNi / 100`였다. worker doc1 probe에서는 consistent paragraph `47/58`, worker doc2 probe에서는 consistent paragraph `118/278`에서 paragraph-level `lineSpacing`을 직접 산출했다.
- `observed`: alignment strongest candidate는 `Ivr.$5n.n4n[styleRef].cUt`다. 기존 worker color-rerun grid와 새 paragraph-style probe를 합치면 `cUt=264 -> toolbar right`, `268 -> center`, `384 -> both/justify` 패턴이 가장 잘 맞는다.
- `observed`: 같은 evidence set에서 `paraStyle.Xli`는 center/right가 모두 `1`인 케이스가 반복돼 direct alignment enum으로 쓰기 어려웠다. 새 probe는 `Xli`를 raw-only로 보존하고 alignment candidate는 `cUt`에만 붙인다.
- `observed`: worker doc1 main text-chain paragraph probe는 resolved alignment candidate `47/58`이 전부 `justify(cUt=384)`로 나왔다. 다만 이 문서의 sampled body paragraph들에서 center/right를 아직 충분히 확보하지 못한 것은 사실이지만, 그 자체를 별도 paragraph branch 가설로 승격할 근거는 없다.
- `observed`: worker doc2 paragraph-style histogram에는 `rawCUt=264` para-style code가 4개 나타나 right candidate가 main text-chain에서도 재현됐다. 다만 sampled consistent paragraph에서는 mixed para-style paragraph가 많아 summary-level resolved alignment count는 `0`으로 남았다.
- `confirmed`: 새 sequence probe `examples/discovery/02-read/probe-align-sequence.ts`를 worker doc1 sample(`xOffset=580,yOffset=140`)에 실행했고, 결과는 `tmp/discovery/02-read/align-sequence-worker-doc1-right-sample.json`에 저장했다.
- `confirmed`: 같은 paragraph에서 `align-left -> align-center -> align-right -> align-both`를 순차 적용하면 `paraStyle.FNi=160`, `LNi=-200`, `wTi.Msi=2`, current run char-style(`cUt=1006895111`, `Xli=5`)는 유지되고, paragraph para-style만 `cUt/Xli`로 바뀌었다. exact observed map은 `left: cUt=260, Xli=6`, `center: cUt=268, Xli=4`, `right: cUt=264, Xli=4`, `both: cUt=384, Xli=24`였다.
- `confirmed`: 위 sequence probe는 마지막에 원래 toolbar align(`both`)으로 복원했고, restored sample도 `cUt=384` bucket으로 돌아왔다. paragraph alignment mutation/read-back verification path로 재사용할 수 있다.
- `observed`: 이후 같은 worker doc1에서 coordinate grid를 다시 스캔한 결과, 현재 stable sample은 주로 `right(cUt=264)`와 `both(cUt=384)`만 재현됐다. `center/left` sample은 기존 좌표가 더 이상 안정적이지 않았다.
- `failed`: rerun artifact `align-sequence-worker-doc1-sample-a-rerun.json`, `align-sequence-worker-doc1-sample-b-rerun.json`, `mutate-align-left-worker-doc1-stable-right.json`은 focus drift가 섞였다. `align-left` 직후 `after` sample이 다른 paragraph(`center`, `FNi=150`)로 튀는 케이스가 생겨 left/center mapping을 same-paragraph mutation으로 재확정했다고 보기는 어렵다.
- `next`: alignment는 `paraStyle.cUt` carrier 자체는 유지하되, current confidence는 `right(cUt=264)`와 `both(cUt=384)` 재확인, `left/center`는 paragraph-anchored revalidation 필요 수준으로 낮춘다.
- `next`: body paragraph 안에서 center/right/justify sample을 더 안정적으로 확보하는 probe가 필요하다. 현재 blocker는 branch split이 아니라 stable body sample coverage다.
- `confirmed`: 새 mutation-diff probe `examples/discovery/02-read/mutate-format-at.ts`를 추가했다. 이 probe는 mutation 전후 `FormatStateProbeReport`, current node `Csi`, referenced `Y5n` char-style entries, current para-style entry, write trace action id를 한 번에 캡처한다.
- `observed`: `UIAPI.getWidgetElementList()`는 formatting toolbar DOM census에는 유용했지만 `getSampleElementListByCmdName()` / `getSampleElementListToDescObj()`는 formatting command payload를 주지 않았다. 또 `SetUIEventListener` trace는 plain caret click만으로는 유의미한 push payload를 남기지 않았다.
- `confirmed`: bold toolbar mutation은 worker doc1 blue sample에서 action id `34051`로 재현됐다. `mutate-bold-toolbar-worker-doc1-blue-node-diff.json` 기준 `toolbar.bold=true -> false` 전환 시 current node `Csi`가 기존 `77/44` entry에서 `115/239` entry로 재배치됐다.
- `confirmed`: 같은 bold mutation에서 기존 bold-on char-style `Y5n[77]`, `Y5n[44]`는 각각 `cUt=1006895110`을 유지했고, 새 bold-off char-style `Y5n[115]`, `Y5n[239]`는 `cUt=1006895108`이었다. current strongest bold carrier는 `runCharStyle.cUt` low-bit 차이 쪽이다.
- `observed`: 같은 bold diff에서 `wTi===bTi.Msi`, `vTi===0`는 mutation 전후에 유지됐다. `wTi/vTi`는 visible bold on/off의 direct carrier 후보에서 더 약해졌다.
- `confirmed`: worker doc1 blue sample의 sequential alignment mutation에서 `align-center`는 action id `34194`, `align-right`는 `34195`로 재현됐다. 두 mutation 모두 run char-style은 유지한 채 paragraph `styleRef`와 para-style raw field만 바뀌었다.
- `confirmed`: 같은 sample의 clean sequential rerun에서 `align-left`는 action id `34193`, `align-both`는 `34192`로 분리됐다. `both -> left`는 `styleRef 164 -> 163`, `paraStyle.cUt 384 -> 260`, `Xli 9 -> 5`였고, `left -> both`는 `styleRef 163 -> 164`, `cUt 260 -> 384`, `Xli 4 -> 10`이었다.
- `confirmed`: alignment mutation evidence를 합치면 current strongest paragraph alignment candidate는 `Ivr.$5n.n4n[styleRef].cUt`이다. worker doc1 blue paragraph 기준 `260 -> left`, `264 -> right`, `268 -> center`, `384 -> both/justify`가 mutation-diff로 재현됐다.
- `observed`: `paraStyle.Xli`는 alignment mutation 중에도 `1/2/3/4/5/8/9/10`처럼 흔들렸다. `Xli`는 direct alignment enum이 아니라 style subclass 또는 secondary layout bitfield일 가능성이 더 높다.
- `confirmed`: italic toolbar mutation은 worker doc1 sample에서 action id `34052`로 재현됐다. `mutate-italic-toolbar-worker-doc1-seq-1.json` 기준 `toolbar.italic=false -> true` 전환 시 same node `Csi`가 `[0,34,1,44] -> [0,34,1,44,10,240]`로 늘었고, 새 `Y5n[240].cUt=1006895111` entry가 생겼다.
- `observed`: italic mutation은 bold처럼 기존 entry를 in-place edit하지 않고 새 char-style code를 붙이는 경향이 있다. 다만 현재 sample은 paragraph end `\\r` run에서 가장 강하게 잡혀 있으므로, italic semantics를 final confirm으로 승격하려면 non-newline run에서도 한 번 더 재현하는 편이 안전하다.
- `failed`: `mutate-align-left/right/both-worker-doc1-clean.json` 세 artifact는 동일 target에 병렬 mutation을 걸어 생성됐다. after-state가 서로 간섭했으므로 alignment action mapping 증거로 승격하지 않는다.
- `observed`: worker doc1 rerun grid(`format-state-grid-worker-doc1-rerun-italic-hunt.json`)에서는 `toolbar.italic=false`인데도 `charStyleCode=240`, `cUt=1006895111`가 남아 있는 좌표가 있었다. doc1 newline-centered italic artifact만으로는 semantics를 고정하면 위험하다는 반례다.
- `confirmed`: worker doc2 clean replay(`mutate-italic-toolbar-worker-doc2-seq-1.json`, `...seq-2.json`)에서 same node `01DCBF443E250D440000C350` / text `보도자료\r`가 `plain -> italic -> plain`으로 왕복했다.
- `confirmed`: 같은 doc2 replay에서 italic on은 `Csi [0,387] -> [0,97]`, `charStyleCode 387(cUt=1006895104) -> 97(cUt=1006895105)`로 바뀌었고, italic off는 정확히 `97 -> 387`로 돌아왔다. current strongest italic carrier는 `runCharStyle.cUt` low-bit difference이며, current italic map candidate는 `5104 -> off`, `5105 -> on`이다.
- `confirmed`: doc2 replay에서도 italic toolbar mutation action id는 `34052`였다. bold (`34051`)와 분리된 direct action id로 재현된다.
- `confirmed`: `mutate-format-at.ts`에 font color mutation(`set-font-color-black/red/blue`)을 추가했고, worker doc2 same node `보도자료\r`에서 clean replay를 확보했다.
- `confirmed`: color mutation은 same node `01DCBF443E250D440000C350` 안에서 `toolbar #000000 -> #FF0000 -> #0000FF -> #000000`로 왕복했다. red mutation artifact는 `mutate-font-color-red-worker-doc2-seq-1.json`, blue는 `...blue...seq-2.json`, black restore는 `...black...seq-3.json`이다.
- `confirmed`: same-node color replay에서 visible font color strongest carrier는 `Y5n[charStyleCode].$Xt`다. observed map은 `#000000 -> $Xt=0`, `#FF0000 -> $Xt=255`, `#0000FF -> $Xt=16711680`이었다. Hancom runtime color int는 RGB가 아니라 BGR-packed order로 보는 쪽이 맞다.
- `observed`: color mutation 동안 `runCharStyle.cUt`는 `1006895104`로 유지됐다. color는 bold/italic과 달리 `cUt` 축이 아니라 `$Xt` 축에서 변한다.
- `observed`: 같은 replay에서 `oqt`는 전부 `12632256(#c0c0c0)`로 고정돼 있었다. `oqt`는 visible font color carrier가 아니라 별도 theme/default/preview 축일 가능성이 더 높다.
- `observed`: color mutation trace에서는 red/blue에 `53635`, black restore에 `34070` + `53635`가 관측됐다. 현재 단계에서는 palette open/apply side-effect가 섞여 있을 수 있으므로, action id는 corroboration 용 raw evidence로만 남긴다.
- `confirmed`: 새 anchored sequence probe는 same-paragraph 판정을 `nodeId` 단독이 아니라 `main Svr.G0i paragraph anchor`로 강제한다. 결과 artifact는 `tmp/discovery/02-read/align-sequence-worker-doc1-right-anchored-v2.json`이다.
- `confirmed`: worker doc1 current body paragraph sample(`xOffset=260,yOffset=140`)에서 same paragraph anchor는 `paragraphIndex=57`, `rawNodeIds=[01DCBF3ADF38E91200001551, ..., 0000019D38A0CAC6D64C0298]`로 유지됐다. 각 step `left -> center -> right -> both`는 전부 `stepValid=true`였다.
- `confirmed`: 위 same-paragraph rerun에서 toolbar align과 `HwpApp.hwpCaret.CSr.M3e.nge.cUt`가 정확히 같이 움직였다. observed map은 `both=384 -> left=260 -> center=268 -> right=264 -> both=384`였다.
- `confirmed`: 같은 artifact에서 node `styleRef=41` 기준 static para-style entry `HwpApp.document.Ivr.$5n.n4n[styleRef]`는 mutation 내내 `cUt=384`, `FNi=40`, `LNi=-5488`로 고정이었다. 즉 mixed body paragraph의 current alignment/readback carrier는 `Ivr.$5n[styleRef]`가 아니라 live paragraph cache `CSr.M3e.nge` 쪽이 더 강하다.
- `observed`: 같은 same-paragraph rerun에서 toolbar `lineSpacing=160`과 `CSr.M3e.nge.FNi=160`은 유지됐지만, static para-style entry `FNi=40`은 그대로였다. current-caret paragraph style read와 text-chain static paragraph snapshot을 같은 carrier로 가정하면 안 된다.
- `confirmed`: production `pageReadStructureFromTextChain()`은 이제 mixed paragraph에서도 `paragraphStyleConsistent=false`를 유지한 채 field-level consensus를 block-level `paragraphStyle`에 올린다. 따라서 `paraStyleVariants[*]`의 `alignment`만 공통이면 `ParagraphBlock.paragraphStyle.alignment`를 채우고, `lineSpacing`만 공통이면 `ParagraphBlock.paragraphStyle.lineSpacing`를 채운다.
- `confirmed`: live SDK read on worker doc1 (`client.document.read()`)에서 paragraph block count는 `52`, paragraph count는 `51`이었다. same-paragraph validation artifact의 `paragraphIndex=57`은 `Svr.G0i` body-chain index이지 SDK paragraph locator가 아니다.
- `confirmed`: 같은 live SDK read에서 mixed paragraph `01DCBF3ADF38E8CC0000154B`는 `paraStyleVariants=[41(lineSpacing=0.4), 144(lineSpacing=1.6)]`로 섞여 있었지만 공통 `alignment=\"justify\"`가 있어 `paragraphStyle={ alignment: \"justify\" }`로 노출됐다. 또 paragraph index `26/34/45` 샘플은 mixed variants여도 `paragraphStyle={ alignment: \"justify\", lineSpacing: 1.6 }`까지 채워졌다.
- `confirmed`: `examples/discovery/02-read/probe-body-branch.ts` rerun artifact `tmp/discovery/02-read/body-branch-worker-doc1-right-current.json`에서 위 current sample은 `kind=\"body\"`, `paragraphIndex=57`로 분류됐다. exact nodeId join against `HwpApp.document.Svr.G0i`가 재현됐다.
- `observed`: earlier preflight artifact `tmp/discovery/02-read/format-state-at-worker-doc1-both-preflight-anchored.json`에서는 `mainTextChainParagraph=null`이 관측됐다. 현재 strongest interpretation은 별도 paragraph branch가 아니라 click drift, break-node hit, 또는 unresolved sample이다.
- `confirmed`: current runtime re-check 기준 `HwpApp.document.Zvr.$bi.G0i`는 `Svr.G0i`와 node set이 동일한 body mirror였고, `HwpApp.document.Svr._Vi.G0i`는 break node 하나만 가진 보조 branch였다. 현재 문서에서는 body 외 별도 text paragraph root 근거가 없다.
- `confirmed`: 이로써 read-side의 큰 불확실성 하나가 정리됐다. paragraph style bonus는 더 이상 `body vs non-body branch split` blocker를 갖지 않고, 현재 남은 핵심은 body paragraph 안에서 stable `center/right` sample coverage를 늘리는 일과 mixed `Aoi` clean-text/table exactness 쪽이다.
- `confirmed`: worker `chrome-9336`에서 `npm run example:chrome-worker-smoke -- text|structure|markdown --worker-id chrome-9336`를 다시 실행했을 때 `\u000b氠瑢u`, `\u000b氠瑢t`, `\u0015湯湷`, `\u001f□ 조사개요`, `\u000b漠杳S`가 현재 SDK read 결과에 그대로 섞여 재현됐다.
- `confirmed`: same worker target에 raw CDP `Runtime.evaluate`로 직접 attach해 `HwpApp.document.Svr.G0i -> tdi`를 재스캔한 결과, noise root cause는 `Aoi`가 pure UTF-16 text buffer가 아니라 text/control/object placeholder payload가 섞인 mixed word buffer라는 점으로 좁혀졌다. 현재 `String.fromCharCode(...Aoi)`는 이 mixed payload를 그대로 노출한다.
- `confirmed`: standalone noise node `01DCBF4FEAE7912C0000C161`는 decoded text가 `\u000b氠瑢u\u000b\r`였고 raw `Aoi=[11,27680,29794,117,0,0,0,11,13,0]`였다. 같은 node에서 `decodeToken(27680,29794)=" lbt"`, `objectId=117`, `HwpApp.document.Ivr.j5n.n4n[*].Qli===117`, `objectNodeId=01DCBF4FEAEA36A20000EF68`, `objectType=6` join이 성립했다. 즉 `u`는 실제 문서 text가 아니라 object placeholder record의 low word를 그대로 UTF-16로 푼 결과다.
- `confirmed`: 같은 family의 standalone node `01DCBF4FEAE790F00000C15A`, `01DCBF4FEAE790F00000C159`, `01DCBF4FEAE790E60000C158`, `01DCBF4FEAE78A600000C0A5`에서도 `Aoi=[11|21,...token...,objectId,0,0,0,repeatControlCode,...]` shape가 반복됐다. token은 `" lbt"`, `"dhgp"`, `" osg"` 등 printable ASCII-like 4-byte 코드로 복원됐고 object/image registry join 여부에 따라 image-bearing / object-only placeholder가 갈렸다.
- `confirmed`: mixed inline noise도 확보됐다. visible text node `01DCBF4FEAE77CC80000BF32`는 decoded text가 `□ 청년층 인구는 \u0015湯湷\u001f\u0015797만 4천명으로 전년동월대비 20만명 감소\r`였고 raw `Aoi` 내부 offset `10`부터 `[21,28271,28279,31,0,0,0,21]` embedded record가 들어 있었다. `decodeToken(28271,28279)="onwn"`, `objectId=31`, `HwpApp.document.Ivr.j5n.n4n[*].Qli===31`, `objectNodeId=01DCBF4FEAEA1D340000EF12`, `objectType=31` join이 성립했다.
- `confirmed`: 위 mixed node는 별도 control node가 아니라 visible sentence를 담는 하나의 text-chain node 자체에 embedded placeholder span이 섞인 케이스다. 따라서 control/object payload는 node boundary만으로 분리되지 않으며, `Aoi` 전체를 text로 보면 반드시 inline noise가 남는다.
- `confirmed`: heading-like noise prefix `\u001f□ 조사개요`도 분리됐다. node `01DCBF4FEAE790A00000C151`의 raw `Aoi=[31,9633,32,51312,49324,44060,50836,13]`, flags `Ooi=4`, `koi=2`, `Hoi=1`, `Moi=1`, `Noi=8`, `Jci=-2147483648`, `idi=0`였고, same-node sliding scan에서는 `Ivr.j5n` / `Ivr.u6n` exact join이 잡히지 않았다. current strongest interpretation은 node head control code `31`이 visible heading text와 함께 decode된 special-char/section-marker bucket이다. semantic label은 아직 미확정이다.
- `confirmed`: current fallback gate가 noise를 통과시키는 이유도 코드와 live sample에서 같이 확인됐다. `pageProbeDocumentTextChain()` / `pageReadStructureFromTextChain()`는 `Aoi` 전체를 UTF-16로 decode한 뒤 `/[가-힣A-Za-z0-9]/`가 하나라도 있으면 text로 취급한다. mixed placeholder는 token decode 뒤에도 Hangul-like garbage나 visible sentence를 같이 포함하므로 이 heuristic을 통과한다.
- `confirmed`: current `parseControlImageRefs()`는 `Aoi`를 offset `0,8,16,...` fixed-step으로만 해석하므로, visible sentence 내부 offset `10`에 박힌 embedded placeholder(`"onwn"`, objectId `31`)를 잡지 못한다. 따라서 현재 structure fallback은 embedded object/control payload를 image/object block으로 분리하지 못하고 일반 paragraph run으로 승격한다.
- `observed`: `Loi[0]` exact child metadata path는 여전히 확인되지만, 이번 three-node sample(`01DCBF4FEAE7912C0000C161`, `01DCBF4FEAE790A00000C151`, `01DCBF4FEAE77CC80000BF32`)에서는 `Loi[0].aui`가 `Aoi` mirror처럼 보였고 `adi.Cci=[]`, `hdi/udi=0-filled`가 유지됐다. current evidence만으로는 `Loi/adi/hdi/udi`가 embedded placeholder span의 canonical discriminator라고 승격할 수 없다.
- `next`: 다음 probe는 `Aoi` embedded placeholder span을 exact offset range로 복구하는 쪽에 집중해야 한다. 최소 요구 출력은 `nodeId`, raw `Aoi`, sliding control record matches, `j5n/u6n` join, standalone vs embedded offset, `Loi[0].aui/fui` mirror 여부다.
- `next`: exact runtime path 후보는 `Aoi` 내부 `(controlCode, tokenLeft, tokenRight, objectId, 0,0,0, repeatControlCode)` family와 `Ivr.j5n.n4n[*].Qli` registry join의 결합이다. semantic label이 없는 control code(`31` 등)는 추측 이름으로 승격하지 않고 raw bucket으로만 보존해야 한다.
- `confirmed`: 새 probe `examples/discovery/02-read/probe-aoi-placeholder-spans.ts` / `src/hancom/discovery/02-read/aoiPlaceholderSpanProbe.ts`를 추가했고, worker doc2/doc1 artifact를 `tmp/discovery/02-read/aoi-placeholder-span-probe-worker-doc2.json`, `aoi-placeholder-span-probe-worker-doc1.json`에 저장했다.
- `confirmed`: 새 probe는 `Aoi`를 sliding 8-word window로 스캔해 exact placeholder family `(controlCode, tokenLeft, tokenRight, objectId, 0, 0, 0, repeatControlCode)`를 복구하고, `objectId -> HwpApp.document.Ivr.j5n.n4n[*].Qli` exact join이 성립하는 span만 recognized placeholder로 분류한다.
- `confirmed`: worker doc2 probe에서는 `traversedNodeCount=636`, `nodesWithRecognizedPlaceholderSpans=84`, `placeholderSpanCount=91`, `embeddedPlaceholderNodeCount=1`, `standalonePlaceholderNodeCount=83`이 관측됐다. same doc2 embedded sample `nodeId=01DCBF443E24F5A20000C114`는 raw `Aoi` offset `10..17`의 `[21,28271,28279,31,0,0,0,21]` span을 제거하면 `cleanTextCandidate="□ 청년층 인구는 797만 4천명으로 전년동월대비 20만명 감소\r"`로 복구됐다.
- `confirmed`: 위 embedded sample에서 `decodeToken(28271,28279)="onwn"`, `objectId=31`, `objectRegistryIndex=31`, `objectNodeId=01DCBF443E2746FE0000F0F4` join이 재현됐다. placeholder span은 node boundary가 아니라 inline offset range로 존재할 수 있다는 결론이 현재 worker state에서도 다시 성립한다.
- `confirmed`: worker doc1 probe에서는 `nodesWithRecognizedPlaceholderSpans=15`, `placeholderSpanCount=16`, `embeddedPlaceholderNodeCount=1`, `standalonePlaceholderNodeCount=14`가 관측됐다. leading sample `nodeId=01DCBF3ADF38E8CC0000154B`는 `dces` / `dloc` span을 제거한 뒤에도 `트 \tࠄĀ   \t\r     \r` 같은 leftover noise가 남았다.
- `observed`: 새 probe의 `nodesWithResidualControlWords`는 doc1/doc2 모두 `0`이었지만, doc1 embedded sample처럼 placeholder span 제거만으로는 설명되지 않는 non-placeholder garbage가 남는 케이스가 있다. current placeholder probe는 strongest family를 exact offset range로 복구했지만, 모든 control-prefix/noise bucket을 해결한 것은 아니다.
- `observed`: `Loi[0].aui` mirror도 여전히 불안정하다. doc2 embedded sample `01DCBF443E24F5A20000C114`는 `firstLoiAuiMatchesAoi=true`였지만, doc1 embedded sample `01DCBF3ADF38E8CC0000154B`는 `false`였다. canonical span metadata path는 아직 없다.
- `confirmed`: current strongest clean-text recovery rule은 `Aoi`에서 recognized placeholder span range를 제거한 뒤 나머지 word를 UTF-16로 decode하는 것이다. 이 규칙은 standalone placeholder node를 paragraph break 수준의 `"\r"`로 정리하고, embedded placeholder node에서는 visible sentence를 복구한다.
- `next`: canonical span metadata path(`Loi[0].aui/fui` 또는 다른 mirror branch)를 찾는 probe가 여전히 필요하다. 현재는 exact offset range recovery는 가능하지만, source of truth는 still sliding scan + registry join이다.
- `next`: placeholder span 제거 후에도 남는 non-placeholder control-prefix bucket(`31` head marker, doc1 leftover garbage 등)을 별도 probe로 좁혀야 한다. clean text exactness는 개선됐지만 아직 완결되진 않았다.
- `confirmed`: 새 probe `examples/discovery/02-read/probe-aoi-control-prefixes.ts` / `src/hancom/discovery/02-read/aoiControlPrefixProbe.ts`를 추가했고, worker doc1/doc2 artifact를 `tmp/discovery/02-read/aoi-control-prefix-probe-worker-doc1.json`, `aoi-control-prefix-probe-worker-doc2.json`에 저장했다.
- `confirmed`: worker doc2에서는 leading control histogram이 `31` 하나로 수렴했고 count는 `4`였다. representative nodes `01DCBF443E250C360000C333`, `...0C2F`, `...0C2B`는 모두 `cleanedWordsPreview=[31,9633,32,...,13]`, `removedPlaceholderSpans=[]`, flags `Ooi=4`, `koi=2`, `Hoi=1`, `Moi=1`, `Noi=8`, `Jci=-2147483648`를 공유했다. current strongest interpretation은 registry join 없는 heading/section-marker family다.
- `confirmed`: 같은 doc2 bucket은 longer sentence node `01DCBF443E2502540000C227`에서도 `cleanedWordsPreview=[31,32,32,31,45,...]`로 재현됐다. 즉 control code `31`은 heading 한 줄뿐 아니라 inline separator/prefix처럼 visible sentence 내부에도 남을 수 있다.
- `confirmed`: worker doc1에서는 leading control histogram이 `11` 하나로 수렴했고 count는 `2`였다. sample nodes `01DCBF3ADF38E8E00000154C`, `0000019D38A0CAC6D64C0298`는 둘 다 `removedPlaceholderSpans=[]`, leading words `[11,27680,29794,28|29,...,11]`, flags `Noi=81/Jci=2560` 또는 `Noi=9/Jci=2048`를 가졌다. current strongest interpretation은 placeholder registry join과 별도인 second-stage control-prefix family다.
- `observed`: doc1 embedded leftover sample `01DCBF3ADF38E8CC0000154B`는 placeholder spans `dces` / `dloc` 제거 후에도 residual weird chars `["ࠄ","Ā"]`가 남았다. 즉 current strongest placeholder removal rule만으로는 모든 garbage를 설명하지 못하고, second-stage control-prefix rule이 필요하다.
- `observed`: control-prefix probe에서도 `Loi[0].aui`는 canonical source로 고정되지 않았다. doc2 leading-31 samples는 `firstLoiAuiMatchesAoi=true`였지만, doc1 leading-11 sample `01DCBF3ADF38E8E00000154C`는 `false`였다.
- `confirmed`: canonical `readStructure()`는 여전히 table exact reconstruction을 하지 않는다. 대신 strongest confirmed runtime path인 current active table cell paragraph read를 `client.experimental.readActiveTableCell()`로 승격했다. 이 surface는 `hwpCaret.AMe.Eni.Aoi`, `hwpCaret.AMe.Eni.sdi.Msi`, `Evr.wVs.vqs[*]`를 이용한 existing exact cell-path를 그대로 노출한다.
- `confirmed`: table registry/control-anchor evidence도 `client.experimental.probeTableCellSemantics()`로 승격했다. probe report는 `Ivr.z5n.n4n`, `Zvr.$bi._Vi.z0i -> Ivr.o6n.n4n[*].zli`, `Svr._ie[*].hie.{abi,Q7i}` join, current caret node id를 그대로 반환한다. 하지만 이 output은 raw evidence surface이며 canonical `TableBlock` reconstruction으로 간주하지 않는다.
- `confirmed`: same probe는 이제 `z5n.PLi` nested descendant count/depth/signature도 함께 남긴다. nested `PLi`가 관측되면 note에 명시하고, downstream은 nested-table candidate를 단일 flat grid로 가정하지 않도록 했다.
- `confirmed`: live repro path는 `tsx examples/sdk-demo.ts ... read active-table-cell`, `tsx examples/sdk-demo.ts ... probe table-semantics`, 기존 discovery script `examples/discovery/02-read/probe-table-cell-semantics.ts`로 남겼다.
- `next`: 다음 좁힐 대상은 두 bucket의 semantic meaning이다. 우선 `31` family는 heading/section-marker 여부, `11` family는 table/object/layout prefix 여부를 raw field(`Noi`, `Jci`, `fui`, neighboring nodes`)와 함께 더 분리해야 한다.
- `confirmed`: 새 probe `examples/discovery/02-read/probe-aoi-bucket-semantics.ts` / `src/hancom/discovery/02-read/aoiBucketSemanticProbe.ts`를 추가했고, worker doc2/doc1 artifact를 `tmp/discovery/02-read/aoi-bucket-semantics-probe-worker-doc2.json`, `aoi-bucket-semantics-probe-worker-doc1.json`에 저장했다.
- `confirmed`: worker doc2 semantic probe에서는 candidate family가 `leading-31`과 `leading-11` 두 개로 수렴했다. `leading-31` family는 `count=4`, shared flags `Ooi=4`, `koi=2`, `Hoi=1`, `Moi=1`, `Noi=8`, `Jci=-2147483648`를 가졌고, sample neighbor context는 `prev="조사대상..." / next="청년층 부가조사 실시..."`, `prev="청년층의 취업실태..." / next="(단위: 천명, %)"`처럼 heading/section 문맥에 모였다.
- `confirmed`: 같은 doc2 `leading-31` family는 longer sentence node `01DCBF443E2502540000C227`에서도 `prev="       < 첫 취업 소요기간 >"` / `next="졸업 후 첫 일자리가..."`로 재현됐다. current strongest interpretation은 control code `31`이 heading line과 section-introducer sentence에 공통으로 붙는 section-marker bucket이라는 쪽이다.
- `observed`: worker doc2 semantic probe에는 별도의 `leading-11` family도 `count=2`로 나타났지만, 이들은 document tail object/control cluster에 붙어 있었고 `prevMeaningfulText="□ 청년층 인구는 797만 4천명으로..."`, `nextMeaningfulText=null`, flags `Noi=41/Jci=2099200` 또는 `Noi=9/Jci=2048`를 가졌다. doc2에서도 `11` family는 heading이 아니라 tail-side control bucket 쪽 해석이 더 강하다.
- `confirmed`: worker doc1 semantic probe에서는 family가 `leading-11` 하나로 수렴했고 `count=2`였다. sample `01DCBF3ADF38E8E00000154C`, `0000019D38A0CAC6D64C0298`는 둘 다 paragraphIndex `57`, `prevMeaningfulText="□ 국토교통부..."`, `nextMeaningfulText=null`였고 flags `Noi=81/Jci=2560` 또는 `Noi=9/Jci=2048`를 보였다. current strongest interpretation은 doc1 `11` family가 paragraph tail / layout-control bucket이라는 쪽이다.
- `observed`: semantic probe 기준 `leading-11` family는 `removedPlaceholderSpans=[]`일 때도 존재하고, doc2 control cluster sample `01DCBF443E24F3AE0000C0DD`처럼 placeholder removal 이후 앞부분에 다시 나타나기도 한다. 따라서 `11` family를 object-registry-backed placeholder의 일부로 합치면 안 된다.
- `next`: 다음 probe는 doc2 `leading-31` bucket의 exact stripping rule을 검증하는 쪽이 유력하다. 현재 evidence는 heading/section marker 해석을 지지하지만, visible text에서 이 marker를 제거해도 paragraph order/neighbor context가 유지되는지 한 번 더 확인할 필요가 있다.
- `confirmed`: 새 probe `examples/discovery/02-read/probe-aoi-section-marker-rewrite.ts` / `src/hancom/discovery/02-read/aoiSectionMarkerRewriteProbe.ts`를 추가했고, worker doc2 artifact를 `tmp/discovery/02-read/aoi-section-marker-rewrite-probe-worker-doc2.json`에 저장했다.
- `confirmed`: worker doc2 rewrite probe에서는 candidate `31` family가 `7`개였고 `allCandidatesRemainMeaningfulAfterStrip31=true`였다. current strongest rewrite rule은 `leading-31` family node의 cleaned word array에서 control code `31`만 제거하고 나머지 word는 그대로 decode하는 것이다.
- `confirmed`: heading node `01DCBF443E250C360000C333`는 `rawText="\u001f□ 조사개요\r"`에서 `stripped31Text="□ 조사개요\r"`로, `01DCBF443E250C0E0000C32F`는 `"□ 추진과정\r"`, `01DCBF443E250BE60000C32B`는 `"□ 조사목적\r"`로 복원됐다.
- `confirmed`: inline sentence case도 같은 rule이 유지됐다. node `01DCBF443E2502540000C227`는 `rawText="\u001f  \u001f- 첫 취업까지..."`에서 `stripped31Text="  - 첫 취업까지..."`로, node `01DCBF443E2501320000C20F`는 `"...기간은\u001f 6개월..."`에서 `"...기간은 6개월..."`로 복원됐다.
- `observed`: doc2 sampled `31` family 전부는 shared `Jci=-2147483648`를 가졌지만 `Ooi/koi/Hoi/Noi`는 heading line과 longer sentence에서 달라졌다. strip rule은 현재 `Noi`나 exact node shape보다 `controlCode 31 present + same doc2 family context` 쪽이 더 강하다.
- `next`: 남은 질문은 이 rule을 exact production rule로 올릴 때 family gate를 어떻게 고정하느냐다. 현재 strongest gate는 `31` bucket + heading/section-marker context evidence지만, canonical metadata path가 아직 없어 final productionization 전엔 one more corroboration이 필요하다.
- `confirmed`: production `readText()` fallback 순서를 바꿔 raw `probeDocumentTextChain()`보다 `pageReadStructureFromTextChain()`를 먼저 사용하도록 연결했다. recognized placeholder family가 제거된 structured snapshot이 존재하면 그 텍스트를 우선 반환한다.
- `confirmed`: production `pageReadStructureFromTextChain()`도 이제 fixed-step image parser만 쓰지 않고 same exact placeholder family를 sliding scan으로 제거한 뒤 paragraph reconstruction을 수행한다. image-bearing span은 기존처럼 direct image block 후보로 유지하고, object-only embedded placeholder도 paragraph text에서는 먼저 제거된다.
- `confirmed`: baseline doc `2025년+사회조사+결과+보도자료+2.hwp` live rerun에서는 earlier garbage family(`ࠄĀ`, `SS789...`)는 재현되지 않았고 residual issue가 `U+001F` control separator로 좁혀졌다. `read text` 기준 control count는 `11`이었다.
- `confirmed`: production `pageReadStructureFromTextChain()`은 placeholder/leading-31 cleanup 뒤에도 남는 residual word `31`을 separator family로 한 번 더 정리한다. current rule은 residual `31` word를 제거하고, raw pattern이 `space + 31 + space`면 앞쪽 space 하나도 같이 제거하는 것이다.
- `confirmed`: same baseline doc live SDK rerun(`target=9F27989F4E001C9F69392003A1E682BD`)에서 `read text` / `export markdown` 모두 `U+001F`가 사라졌고, representative strings `노동 ○ 10개 부문`, `그다음은 사회적 편견`, `문화예술·스포츠`가 현재 output에 그대로 재현됐다.
- `confirmed`: 새 probe `src/hancom/discovery/02-read/tableCellSemanticsProbe.ts`와 example `examples/discovery/02-read/probe-table-cell-semantics.ts`를 추가했다. 목적은 `z5n` structural signature와 current `z0i/_ie -> o6n.zli` join을 같은 step log로 묶어 다음 live run에서 exact table cell semantics를 바로 비교 가능하게 만드는 것이다.
- `confirmed`: live probe `npx tsx examples/discovery/02-read/probe-table-cell-semantics.ts --port 9333 --target-id A3E565BFDE99512E6DE605079EAD9D50 3 3 9 4`와 `--port 9223 --target-id 4BFDC6337D7008E6F615ACAF7864F860 2 2 4 4`, `3 3 9 4`를 실행했고 결과를 `tmp/discovery/02-read/table-cell-semantics-youth-survey-worker-9333-3x3-9f-4r.json`, `table-cell-semantics-housing-9223-2x2-4f-4r.json`, `table-cell-semantics-housing-9223-3x3-9f-4r.json`에 저장했다.
- `confirmed`: 세 artifact 모두에서 `z5n.n4n[*].PLi[*]` document-level 구조 snapshot, `Zvr.$bi._Vi.z0i -> Ivr.o6n.n4n[*].zli` join, `Svr._ie.hie.{abi,Q7i} -> Ivr.o6n.n4n[*].zli` join은 같은 tick에서 재현됐다.
- `observed`: 그러나 exact table cell ordering은 아직 닫히지 않았다. youth-survey `3x3`에서는 caret node id가 step마다 이동해도 top-level `z0i` carrier는 주로 두 `qli/cUt` 쌍 사이만 반복했고, `Svr._ie.hie.{zXi,tyi}`와 `abi` join head도 대부분 고정된 채 row/col ordinal처럼 분화되지 않았다.
- `observed`: housing `2x2`/`3x3`에서도 같은 문제가 재현됐다. boundary step 전후로 `carrier`/`_ie.length`는 phase처럼 바뀌지만, 이 값만으로는 현재 cell의 row/column을 deterministic하게 복원할 수 없었다.
- `confirmed`: production warning을 더 정확하게 바꿨다. 이제 `pageReadStructureFromTextChain()`는 `Ivr.z5n.n4n`만 있는 경우 `o6n/selection bridge unavailable`을, `z5n + o6n + Zvr.$bi/Svr._ie`가 함께 보이는 경우에는 `stable row/cell ordering unresolved`를 명시적으로 경고한다.
- `confirmed`: pure test를 추가해 두 warning branch를 고정했다. `tests/pageFunctions.readStructure.test.ts`는 `bridge unavailable` 케이스와 `join exists but ordering unresolved` 케이스를 각각 검증한다.
- `confirmed`: `TableCellSemanticsProbeReport`에 step fingerprint를 추가했다. 새 fingerprint는 `currentCarrier`, `carrierLineage`, `ieHeadSignature`, `z5nSignature`를 같은 tick에서 바로 노출해서 artifact diff가 raw carrier 배열 전체를 다시 파싱하지 않아도 되게 만든다.
- `observed`: regenerated live artifacts 기준 `ieHeadSignature`는 step 간 거의 고정이고, `z5nSignature`도 run 내부에서 변하지 않았다. 반대로 현재 tick의 정보량은 주로 `currentCarrier + carrierLineage` rotation에 집중된다. next probe는 `_ie` count보다 lineage rotation과 caret progression을 먼저 row/col candidate로 대조하는 쪽이 더 유력하다.
- `confirmed`: pure helper `src/utils/tableCellSemantics.ts`를 추가했고 `summarizeTableCellSemanticsWalk()`가 step bucket/state transition을 직접 계산한다. discovery example `examples/discovery/02-read/probe-table-cell-semantics.ts`도 이제 `summary.forward/reverse/combined`를 artifact에 같이 기록한다.
- `observed`: latest housing `2x2` rerun summary 기준 `stableZ5nSignature=true`, `stableIeHeadSignature=false`, `uniqueCurrentCarrierCount=1`, `uniqueStateCount=3`이었다. 즉 current top-level carrier 단독보다 `carrierLineage` state bucket이 훨씬 설명력이 높고, next exact-label probe는 lineage state와 caret progression을 직접 대응시켜야 한다.
- `next`: current strongest blocker는 `table present` 여부가 아니라 `exact row/cell ordering`이다. 다음 probe는 `z0i` carrier 집합 전체의 step-wise rotation과 caret progression을 cell matrix candidate로 정규화할 수 있는지 좁히는 쪽이어야 한다.
- `confirmed`: 기존 worker artifact `tmp/discovery/02-read/table-cell-semantics-worker-doc2-2x2-4forward-4reverse.json`, `table-cell-semantics-youth-survey-worker-9333-3x3-9f-4r.json`, `table-cell-semantics-housing-9223-2x2-4f-4r.json`를 재검토했다. 세 artifact 모두 every step에서 `selectionCarriers[*].registryMatches`가 `Ivr.o6n.n4n[*].zli`로 resolve됐고, `svrIePointers[*].abiMatches/q7iMatches`도 같은 registry family를 유지했다.
- `confirmed`: same artifacts 기준 `z5nStructures` snapshot은 각 run 내부에서 끝까지 stable했다. doc2 worker 문서는 전 step에서 `z5nStructures.length=10`, housing 문서는 전 step에서 `3`으로 고정됐고, caret traversal이나 reverse walk가 `z5n` document-level structural map을 바꾸지는 않았다.
- `confirmed`: 반면 current selection-side carrier는 step마다 rotation했다. worker doc2 `2x2` / `3x3` run과 housing `2x2` run 모두 first carrier registry target set이 각기 `3`개 bucket 사이에서만 바뀌었고, caret node id도 step마다 이동했다. current strongest interpretation은 `z0i -> o6n.zli`가 current cell/neighborhood rotation을 주고, `z5n`은 document-level table map을 유지한다는 쪽이다.
- `observed`: `_ie` cardinality는 여전히 history-sensitive였다. worker doc2 `2x2`는 `5 -> 48` burst 후 reverse에서도 `48`이 유지됐고, worker doc2 `3x3`는 run 전체가 `48`로 고정됐으며, housing `2x2`는 `5 -> 12`로만 올라갔다. `_ie.length`나 `hie.zXi/tyi`를 row/col/current-cell ordinal로 직접 승격하면 안 된다.
- `confirmed`: 새 live rerun `npx tsx examples/discovery/02-read/probe-table-cell-semantics.ts --port 9333 --target-id DC62DD2C3C8AAA0CFD3B4E2DC0C4520B 2 3 6 4`와 `... 3 2 6 4`를 실행했고 결과를 `tmp/discovery/02-read/table-cell-semantics-housing-worker-9333-2x3-6f-4r.json`, `table-cell-semantics-housing-worker-9333-3x2-6f-4r.json`에 저장했다.
- `confirmed`: housing rectangular rerun에서도 `z5nStructures`는 `2x3`와 `3x2`가 완전히 같았다. 두 run 모두 `z5nStructures.length=3`, entry `(Qli,Xli,cUt,pliSignature)`가 동일했고 traversal 전체에서도 변하지 않았다. current strongest interpretation은 `z5n`이 current row/col이나 inserted table dimensions를 직접 주지 않는 stable document-side structure map이라는 쪽이다.
- `confirmed`: 반대로 rectangular rerun의 `selectionCarriers -> registryMatches` set은 dimension-sensitive하게 갈렸다. `2x3`는 in-grid phase에서 `[27,28,29,30,3,4]` set을 유지하다가 overflow/late phase에서 `[11,31,32,33,3,53,54,55]`로 바뀌었고, `3x2`는 같은 baseline에서 overflow 후 `[10,11,31,3,53,54,55,56]`, reverse mid-phase에서는 `[31,32,33,3,53,54,55,56]`로 이동했다.
- `observed`: same rectangular rerun에서 `_ie` cardinality는 두 run 모두 `5 -> 12`로만 변했고, 차이는 carrier registry family 쪽에서 드러났다. current table-cell probe에서 dimensions를 더 직접 반영하는 쪽은 `_ie.length`보다 `selectionCarriers` registry target set이다.
- `confirmed`: 새 pure helper `src/hancom/discovery/02-read/tableCellWalkAnalysis.ts`, example `examples/discovery/02-read/analyze-table-cell-ordering.ts`, test `tests/tableCellWalkAnalysis.test.ts`를 추가했다. 목적은 saved walk artifact에서 `selectionRegistryFamily`, `lineageRegistryPaths`, `ieCount`, stable `z5n/ieHead` signature를 정규화해 contiguous phase segment로 바로 비교하게 만드는 것이다.
- `confirmed`: `npx tsx examples/discovery/02-read/analyze-table-cell-ordering.ts tmp/discovery/02-read/table-cell-semantics-housing-worker-9333-2x3-6f-4r.json tmp/discovery/02-read/table-cell-semantics-housing-worker-9333-3x2-6f-4r.json` 결과를 `tmp/discovery/02-read/table-cell-ordering-analysis-housing-rectangular.json`에 저장했다.
- `confirmed`: analysis artifact 기준 두 rectangular run의 shared selection family는 baseline `[27,28,29,30,3,4]` 하나뿐이었다. `2x3`는 unique late family `[11,31,32,33,3,53,54,55]`를, `3x2`는 overflow family `[10,11,31,3,53,54,55,56]`와 reverse-mid family `[31,32,33,3,53,54,55,56]`를 별도로 가졌다.
- `confirmed`: same analysis artifact는 phase segment를 contiguous range로 고정했다. `2x3`는 `forward step0..3 baseline -> step4 late-phase -> step5 baseline -> step6 overflow`, `reverse step1..4 baseline`으로, `3x2`는 `forward step0..5 baseline -> step6 overflow -> reverse step0..1 overflow -> reverse step2..4 mid-phase`로 요약된다. 다음 probe는 raw step log 전체보다 이 phase segmentation을 current matrix candidate와 직접 대조하는 쪽이 더 적합하다.
- `confirmed`: 새 live probe `examples/discovery/02-read/probe-table-cell-tab-order.ts`를 추가했다. 이 probe는 fresh inserted table에 `T00..` label을 current cell에 직접 써 넣고, forward/reverse walk에서 `pageReadCurrentTableCellState()`와 `pageProbeTableCellSemantics()`를 같은 step에 묶는다. 목표는 registry family를 actual Tab-order cell index와 직접 붙이는 것이다.
- `confirmed`: `npx tsx examples/discovery/02-read/probe-table-cell-tab-order.ts --port 9333 --target-id DC62DD2C3C8AAA0CFD3B4E2DC0C4520B 2 3`와 `... 3 2`를 실행했고 결과를 `tmp/discovery/02-read/table-cell-tab-order-housing-worker-9333-2x3.json`, `table-cell-tab-order-housing-worker-9333-3x2.json`에 저장했다.
- `confirmed`: 두 tab-order artifact 모두 reverse labels가 정확히 `T05 -> T00`로 읽혔다. 즉 same run 안에서 at least Tab traversal order와 reverse traversal order 자체는 artifact 안에서 self-consistent하게 고정된다.
- `confirmed`: `2x3` tab-order artifact 기준 current carrier lineage head는 `T00/T01/T03 -> o6n[58]`, `T02/T04 -> o6n[19]`, `T05 -> o6n[56]`로 갈렸다. selection family도 `T00/T01/T03`에서 `[31..33,55..58]`, `T02/T04`에서 `[15..22]`, `T05`에서 `[31..33,54..58]`로 나뉘었다.
- `confirmed`: `3x2` tab-order artifact 기준 current carrier lineage head는 `T00/T01/T02 -> o6n[58]`, `T03/T05 -> o6n[56]`, `T04 -> o6n[19]`로 갈렸다. 즉 `o6n[19]` family와 `o6n[56]` family가 rectangular shape에서 서로 다른 Tab-order slot에 붙는다.
- `observed`: 새 tab-order analysis artifact `tmp/discovery/02-read/table-cell-ordering-analysis-housing-tab-order.json` 기준 shared family는 `[31..33,55..58]`, `[15..22]`, `[31..33,54..58]` 세 개로 수렴했지만 segment placement는 shape마다 달랐다. `2x3`는 `[15..22]` family가 `T02/T04`에, `3x2`는 `T04` 단일 step에만 나타났다. current strongest interpretation은 family 자체보다 family가 나타나는 step segment가 row/col semantics에 더 가깝다는 쪽이다.
- `confirmed`: 새 live probe `examples/discovery/02-read/probe-table-cell-arrow-order.ts`를 추가했고, housing worker target에서 `2x3 right,right,down,left,left`, `3x2 right,down,down,left,right` 시퀀스를 실행해 `tmp/discovery/02-read/table-cell-arrow-order-housing-worker-9333-2x3.json`, `table-cell-arrow-order-housing-worker-9333-3x2.json`에 저장했다.
- `confirmed`: `3x2` arrow artifact에서는 `ArrowRight` 후 `T01`, `ArrowDown` 후 `T03`, 다시 `ArrowDown` 후 `T05`, `ArrowLeft` 후 `T04`, `ArrowRight` 후 `T05`가 재현됐다. current strongest interpretation은 at least this shape/run에서는 `Escape -> Arrow`가 실제 cell-to-cell traversal을 만들었다는 쪽이다.
- `confirmed`: same `3x2` arrow artifact에서 family도 label과 함께 갈렸다. `T01`은 `[31..33,55..58]` + lineage `o6n[58]`, `T03/T05`는 `[15..22]` or `[31..33,54..58]` + lineage `o6n[19]/o6n[56]`로 분리됐다. 즉 non-Tab movement에서도 carrier family가 current traversal slot과 함께 바뀌는 positive evidence가 생겼다.
- `observed`: 그러나 `2x3` arrow artifact는 더 noisy했다. `ArrowRight` 두 번째부터 lineage/family는 `o6n[19]` 쪽으로 바뀌었지만 current cell text는 계속 `T01`로 남았다. current strongest interpretation은 Arrow probe가 shape/run에 따라 `semantics carrier`는 움직여도 `pageReadCurrentTableCellState()` text path는 stale할 수 있다는 것이다.
- `observed`: `3x2` start label은 `T00`가 아니라 `PT00`로 읽혔다. 이는 discovery label write가 clean replace가 아니라 append-like artifact를 남길 수 있음을 뜻한다. Arrow probe는 movement correlation 용으로는 유용하지만, cell text exactness source로 승격하면 안 된다.
- `next`: 다음 단계는 `3x2`처럼 Arrow movement가 label과 같이 움직이는 positive shape를 고정 fixture로 삼고, same target에서 click-seed 또는 alternate shape rerun으로 재현성을 확인하는 것이다. current blocker는 non-Tab evidence가 "있냐 없냐"가 아니라 "언제 stable하고 언제 stale한지"를 분리하는 쪽으로 바뀌었다.
- `next`: 다음 probe는 current `Tab-order index -> carrier family/lineage` mapping을 같은 dimensions의 arrow-key 또는 click seed movement와 교차대조하는 것이다. 이제 blocker는 "family를 current cell에 붙일 수 있나"가 아니라 "붙은 family를 row-major/col-major 축 중 어디에 놓을지"로 더 좁혀졌다.
- `next`: 다음 단계는 current rotation bucket `z0i/o6n.zli` 3개와 stable `z5n.PLi` cluster를 같은 table에서 row/col/cell label과 직접 대조하는 것이다. 즉 probe 질문은 더 이상 "join이 있나"가 아니라 "어느 field가 row, col, boundary, traversal phase를 맡나"로 좁혀진다.
